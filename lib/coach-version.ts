/** Bump when coach rules/payload change; older reviews suggest Regenerate. */
export const COACH_LOGIC_VERSION = 4;

export const DEFAULT_COACH_PROFILE = `Portfolio unrealized P&L includes long-term stock holdings I am not closing soon. Coach me on options margin, greeks, and name concentration — not on "reducing" whole-portfolio MTM.`;
